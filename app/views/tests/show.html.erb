<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Index | Simpler</title>
  </head>
  <body>
    <h1>SHOW in action!</h1>
    <p><%= @time %></p>
    <p><%= @params %></p>
    <ul>
      <% @tests.each do |test| %>
        <li><%= test.title%> ( <%= test.level%> )</li> 
      <% end %>
    </ul>
  </body>
</html>